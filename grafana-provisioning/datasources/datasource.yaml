apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    jsonData:
      httpHeaderName1: Authorization
    secureJsonData:
      httpHeaderValue1: Token 

    access: proxy
    database: 
    url: 
    isDefault: true
    #editable: true

