apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    jsonData:
      httpHeaderName1: Authorization
    secureJsonData:
      httpHeaderValue1: Token $TOKEN

    access: proxy
    database: $BUCKET
    url: $URL
    isDefault: true
    #editable: true

